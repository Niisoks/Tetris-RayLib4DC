#pragma once

#include <dc/maple/vmu.h>
#include <dc/maple.h>

class VmuManager {
public:
    VmuManager();
    ~VmuManager();
    void displayImage(const char *xmp);
    void resetImage();

private:
};